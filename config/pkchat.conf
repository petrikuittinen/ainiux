# pkchat system-wide defaults
#
# Installed as /etc/xdg/pkchat/config.conf by default. A user config at
# $XDG_CONFIG_HOME/pkchat/config.conf (normally ~/.config/pkchat/config.conf)
# may override individual settings.
#
# This is pkchat's deliberately small TOML-alike format, not full TOML.
# Keep secrets out of this file. Select an environment variable or key file
# under [credentials] instead of storing an API key value here.

config_version = 1
provider = openai
model =
api = chat

[endpoint]
base_url =
chat_url =
models_url =
responses_url =

[generation]
stream = true
# temperature, top_p, and max_output_tokens are intentionally unset so the
# provider/model defaults remain effective.

[context]
window_tokens = 0
max_bytes = 0
policy = error

[network]
connect_timeout_seconds = 10
request_timeout_seconds = 0
proxy =
insecure_tls = false

[credentials]
key_env =
key_file =

[output]
format = text
render_format = md

[input]
max_input_bytes = 1048576
max_image_bytes = 20971520
image_capability = auto

[editor]
undo_limit = 5
huge_file_size_warning = 1073741824
file_size_limit = -1
# assist_behavior = "This is a one-shot editor assist task..."
# assist_spell = "Fix spelling errors only. Do not change wording, grammar, punctuation style, or meaning."
# assist_grammar = "Fix grammar errors only. Do not change wording, spelling where it is already correct, or meaning."
# assist_continue = "Continue the text naturally from where it ends."
# assist_fact = "Fact-check the text and add brief comments about any factual issues you find."

# Custom editor AI commands use repeatable [command] sections. Each block needs
# string, modes, and prompt. Built-in commands include /spell, /grammar,
# /continue, /fact, /comment, /rewrite, /English, /Chinese, and /Finnish. They
# default to selection, all, continue, and insert. Config modes are selection,
# all, continue, insert, and fact. local_insert is accepted as an alias for
# insert. A matching string replaces a built-in command; new strings add commands.
#
# [command]
# string = /example
# modes = all, selection, continue, insert
# prompt = "Output 5 examples of the user-given topic. Answer without any preamble and within <content>...</content> tags."

[url_fetch]
max_bytes = 1048576
allow_private_addresses = false

[tui]
colors = true
theme = dark
thinking_traces = false

# Repeatable [Model-setting] blocks map model name patterns and purposes to
# sampling defaults. Wildcard suffixes such as Qwen3.6-* are matched when
# purpose-based model selection is wired up. Optional thinking_budget accepts
# either a token count (for example 8192) or a verbal label (for example high).

[Model-setting]
model = Qwen3.6-*
purpose = creative
default_system_prompt = ""
temperature = 1.0
top_k = 20
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0

[Model-setting]
model = Qwen3.6-*
purpose = coding
default_system_prompt = ""
temperature = 0.6
top_k = 20
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0

[Model-setting]
model = Qwen3.6-*
purpose = instruct
default_system_prompt = ""
temperature = 0.7
top_k = 20
top_p = 0.80
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 1.5

[Model-setting]
model = Qwen3.6-*
purpose = general
default_system_prompt = ""
temperature = 0.8
top_k = 20
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 1.5

[Model-setting]
model = Qwen3.5-*
purpose = coding
default_system_prompt = ""
temperature = 0.6
top_k = 20
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0

[Model-setting]
model = Qwen3.5-*
purpose = instruct
default_system_prompt = ""
temperature = 0.7
top_k = 20
top_p = 0.8
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 1.5

[Model-setting]
model = Qwen3.5-4B
purpose = general
default_system_prompt = ""
temperature = 1.0
top_k = 20
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 1.5

[Model-setting]
model = Gemma-4-31B
purpose = general
default_system_prompt = ""
temperature = 1.0
top_k = 64
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0

[Model-setting]
model = Nemotron-3-Nano-Omni-30B-A3B
purpose = general
default_system_prompt = ""
temperature = 0.6
top_k = 0
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0

[Model-setting]
model = DeepSeek-V4-Flash
purpose = general
default_system_prompt = ""
temperature = 1.0
top_k = 0
top_p = 1.0
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0

[Model-setting]
model = Llama-3.1-8B
purpose = general
default_system_prompt = ""
temperature = 0.6
top_k = 40
top_p = 0.9
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0

[Model-setting]
model = Devstral-Small-2-24B
purpose = coding
default_system_prompt = ""
temperature = 0.15
top_k = 20
top_p = 0.95
min_p = 0.0
repeat_penalty = 1.0
presence_penalty = 0.0
