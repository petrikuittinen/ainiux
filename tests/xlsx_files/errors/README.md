# XLSX error fixtures

Every file here must fail conversion without returning partial Markdown.

- `truncated.xlsx` — truncated ZIP
- `utf8entity.dontuse` — hostile/invalid XML entities
- `sec-j47r.dontuse` — security/odd package that must not crash
